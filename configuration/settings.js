'use strict'

const settings = {
    debugRequests: true,
    responseTimeThreshold: 500,
    version: 'v1.0',
    apiEndpoint: '/v1/api',
    env: {
        API_PORT: 3000,
        API_HOST: 'localhost'
    }
}

module.exports = settings
